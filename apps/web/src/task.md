# Refactoring Task Progress

- [x] Reorganize directories and create structural target folders
- [x] Move UI primitives in `src/components/ui/` and base card/layouts to `src/shared/ui/`
- [x] Move core layouts (`dashboard-shell`, `marketing-layout`, `auth-shell`) to `src/app/layouts/`
- [x] Move core providers (`auth-context`, `theme-provider`) to `src/app/providers/`
- [x] Refactor API functions in `src/lib/api.ts` into `src/shared/api/`
- [x] Relocate hooks and utilities to `src/shared/hooks/` and `src/shared/utils/`
- [x] Restructure routes entrypoints in `src/routes/` to import feature page modules
- [x] Validate project compiles successfully
